<div
        class="card mt-3 mt-sm-4 mt-md-0 mt-lg-4 font-size-3"
>
    <div
            class="card-header font-size-3 text-uppercase font-weight-bold d-flex px-3 px-xl-4"
    >
        {{ 'Top partners'|t }}

        <a
                href="#"
                class="ml-auto card-view-all font-weight-normal text-grey text-decoration-none text-nowrap"
        >{{ 'Full list'|t }}
            <i class="icon-arrow-right2 text-dark-silver"></i>
        </a>
    </div>
    <div class="card-body p-3 py-md-3 px-xl-4">
        <div class="tab-content" id="myTabContent">
            <div
                    class="tab-pane fade show active"
                    id="daily"
                    role="tabpanel"
                    aria-labelledby="daily-tab"
            >

                <ul class="list-group list-group-flush mt-2">
                    {% set i=0 %}
                    {% for partner in leadPartner %}
                        {% set i=i+1 %}
                        {% set fullname = partner.name %}
                        {% set fullname = fullname|split(' ') %}
                        {% set name = fullname[0] %}
                        {% set lastName = fullname[1] %}
                        <li
                                class="list-group-item d-flex align-items-center px-0"
                        >
                                <span
                                        class="font-size-6 font-size-xl-7 text-dark-silver mr-2 mr-xl-3"
                                >{{ i }}</span
                                >
                            <div class="d-flex w-100 align-items-center">
                                <a
                                        href="#"
                                        class="rounded-circle overflow-hidden d-inline-block"
                                >
                                    <img
                                            src= {{ image_style_url(partner.img, 'partner_image') }}
                                            class="rounded"
                                            alt="..."
                                    />

                                </a>
                                <h6
                                        class="text-uppercase line-height-1-2 font-size-3 font-size-xl-base mb-0 mx-2 mx-xl-3"
                                >
                                    <a
                                            href="#"
                                            class="text-decoration-none d-inline-block text-hover-success"
                                    >
                                        {{ name }}
                                        <span class="d-block font-weight-bold"
                                        >{{ lastName }}</span
                                        >
                                    </a>
                                </h6>
                                <span
                                        class="text-success font-size-4 font-size-xl-4 font-weight-bold ml-auto"
                                >{{ partner.donation }} ₾</span
                                >
                            </div>
                        </li>

                    {% endfor %}
                </ul>
            </div>
            <div
                    class="tab-pane fade"
                    id="weekly"
                    role="tabpanel"
                    aria-labelledby="weekly-tab"
            >
                weekly
            </div>
            <div
                    class="tab-pane fade"
                    id="monthly"
                    role="tabpanel"
                    aria-labelledby="monthly-tab"
            >
                monthly
            </div>
            <div
                    class="tab-pane fade"
                    id="total"
                    role="tabpanel"
                    aria-labelledby="total-tab"
            >
                total
            </div>
        </div>
    </div>
</div>