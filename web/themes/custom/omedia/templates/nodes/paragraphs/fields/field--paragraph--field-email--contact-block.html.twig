{% if items%}
    {% for item in items %}
        <div class="cb-row clearfix">
            {{ svg_icon('email', 'cb-icon') }}
            <div class="cb-text">
                <a href="mailto:{{ item.content }}">{{ item.content }}</a>
            </div>
        </div>
    {% endfor %}
{% endif %}
