{% if items%}
    {% for item in items %}
        <div class="cb-row clearfix">
            {{ svg_icon('location', 'cb-icon') }}
            <div class="cb-text">
                {{ item.content }}
            </div>
        </div>
    {% endfor %}
{% endif %}
